@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model Application

<h1>Dating Application</h1>

<form method="post">    @* Post this information from the form to the program, that's what POST is here for,
                           in that way after hitting submit it won't post the information to the URL*@

    <input type="hidden" asp-for="ApplicationId" /> 
    @* We didn't have the input field before for ApplicationId but we had to add it after add the Edit capability
       so when we edit the ApplicationId that is clicked on is what's kept and helps update the record rather
       than creating a new record with new ApplicationId, which is what will happen if we didn't add this field *@
    <label asp-for="FirstName">First Name:</label>
    <input asp-for="FirstName" />
    <br />
    <label asp-for="LastName">Last Name:</label>
    <input asp-for="LastName" />
    <br />
    <label asp-for="Age">Age:</label>
    <input asp-for="Age" />
    <br />
    <label asp-for="PhoneNumber">Phone:</label>
    <input asp-for="PhoneNumber" />
    <br />
    <select asp-for="MajorId">
        <option value="">Select a Major</option>
        @foreach (Major m in ViewBag.Majors)
                {
                    <option value="@m.MajorId">@m.MajorName</option>
                }
            </select>
    <br />
    <label asp-for="CreeperStalker">Are you a Creeper and/or a Stalker?</label>
    <select asp-for="CreeperStalker">
        <option value="true">Yes</option>
        <option value="false">No</option>
    </select>
    <br />

    @* 
       After bringing in the model Application.cs and adding those asp-for...
       now this DatingApplication.cshtml is bound to Application.cs so whenever
       someone submit the form we get an instance of the Application.cs for it
    *@

    <button type="submit">Submit Application</button>

    <div asp-validation-summary="All"></div>    
    @* this above will generation error messages if user don't fill out required fields *@

</form>