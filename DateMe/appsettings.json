{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DatingConnection": "Data Source = DatingApplications.sqlite" //this line will create a database with the name DatingApplications.sqlite or if the database exists we can assign the name to Data Source.
  }
}
